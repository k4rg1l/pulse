# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python

# Virtual envs
.venv/
venv/
env/
ENV/

# Build artifacts
build/
dist/
*.spec.bak

# IDE / editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Secrets and local state (never commit)
secrets.py
_local_secrets.py
.env
.env.*
settings.json
state.json

# Claude Code: track the shared security-review hook config; ignore local-only
# overrides. The bare `settings.json` pattern above would otherwise swallow it.
!.claude/settings.json
.claude/settings.local.json

# Research probe output — contains real account usage/activity data, never commit
tools/_probe_out/

# Postman extension workspace sync (OpenAI/Cline/OpenRouter collections) — local tooling, not project source
.postman/
postman/

# Scratch / capture files used during development
.run.err
.run.log
.vrun.err
.vrun.out
.vrun.*
_capture.py
.2nd.err
.2nd.log
.dash*.png
.mvp.png
.menu.png
.tip.png
.final.png
*.png.bak

docs/v41-*.png
docs/_*.png
